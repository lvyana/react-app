/**
 * @file 创建按钮
 * @author ly
 * @createDate 2023年2月25日
 */
import React, { useState } from 'react';

const CreatButton = () => {
	const [buttonList, setButtonList] = useState([]);

	return <div>CreatButton</div>;
};

export default CreatButton;
